<Page x:Class="ApmMedical.Views.AuthUsersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApmMedical.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
     Title="Авторизация">

    <Grid>


        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">


            <TextBlock>Email</TextBlock>
            <TextBox Style="{StaticResource FieldInput}" x:Name="LoginTextBox">renat@mail.ru</TextBox>
            <TextBlock>Пароль</TextBlock>
            <PasswordBox Style="{StaticResource FieldInput}"  x:Name="TextPasswordBox"  Password="Renat3*"></PasswordBox>

            <Button Style="{StaticResource AuthButton}" x:Name="SignInButton"   Click="SignInButton_Click"   Content="Войти"></Button>


        </StackPanel>
    </Grid>

</Page>